from django.urls import path

from . import views

urlpatterns = [
    # path("login/", views.login_view, name="login-internal"),
    # path("register/", views.register_user, name="register-ntneral"),
    # path("logout/", views.logout_view, name="logout-internal"),
]
